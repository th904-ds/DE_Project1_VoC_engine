# 이 파일은 예시입니다. 실제 값은 DB/.env 에 작성하세요 (이 폴더의 상위 디렉터리).
# task6_absa.py 는 DB/.env 를 자동으로 읽습니다.

MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=Cluster0
MONGO_DB=musinsa_db
MONGO_COLLECTION_CLEAN=reviews_clean
